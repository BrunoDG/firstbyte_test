import React from 'react';
//import { reverse } from 'django-js-reverse';

const RecipeEntry = () => (
  <div>
    <h1>Recipe Entry</h1>
    {/* Add your recipe entry form or logic here */}
    {/*<a href={reverse('recipe_entry', { param: 'value' })}>Link to Recipe Entry</a>*/}
  </div>
);

export default RecipeEntry;
