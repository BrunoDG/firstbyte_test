import React from 'react';
//import { reverse } from 'django-js-reverse';

const RecipeView = () => (
  <div>
    <h1>Recipe View</h1>
    {/*<a href={reverse('recipe_view', { param: 'value' })}>Link to Recipe View</a>*/}
  </div>
);

export default RecipeView;
